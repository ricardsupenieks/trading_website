{% extends 'navigation.twig' %}

{% block content %}
    <div class="flex items-center justify-center h-screen bg-gray-200">
    <form action="/sign-in" method="post" class="w-full max-w-sm">
        <div class="flex items-center border-b border-b-2 border-teal-500 py-2">
            <input class="appearance-none bg-transparent border-none w-full text-gray-700 mr-3 py-1 px-2 leading-tight focus:outline-none" type="text" name="email" id="email" placeholder="E-mail" required>
            <i class="fa fa-envelope text-gray-500"></i>
        </div>
        <div class="flex items-center border-b border-b-2 border-teal-500 py-2">
            <input class="appearance-none bg-transparent border-none w-full text-gray-700 mr-3 py-1 px-2 leading-tight focus:outline-none" type="password" name="password" id="password" placeholder="Password" required>
            <i class="fa fa-lock text-gray-500"></i>
        </div>
        <div class="flex items-center justify-between mt-8">
            <input type="submit" id="submit" value="Submit" class="bg-teal-500 hover:bg-teal-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline-teal">
            {% if errors.userCredentials is same as (false)%}
                <p class="text-red-500 text-xs italic">Incorrect Credentials</p>
            {% endif %}
        </div>
    </form>
    <div class="register mt-8"><a href="/register" class="text-teal-500 hover:text-teal-800 font-bold">Register</a></div>
    </div>
{% endblock %}
